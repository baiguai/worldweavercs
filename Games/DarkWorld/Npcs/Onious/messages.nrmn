{link, parent=onious
    {enter_message
        {logic
            logic={@
                ?(onious)((travel))active = 'true'
            }
        }
        {message
            output={@
<<[self]((npc_label_the_proper))>> follows you.
            }
        }
    }


    --   Only show her name if the player knows it.
    {look
        {message
            output={@
                You see <<[self]((npc_label_a))>>.
            }
        }
    }
}
