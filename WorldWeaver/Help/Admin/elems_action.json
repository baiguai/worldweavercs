{
    "topics" : [
        {
            "pattern" : "\\b(?:action.element)\\b",
            "title" : "Action Element",
            "string" : "
Actions are extremely powerful elements. If you consult the:
~/Config/ElementProcRules/ElementByType.json
configuration file, you will see all the different element types that actions
can process. Actions can also call custom type elements or even cause the
player to die.

---

For a growing list of action element examples use:
_help action examples
"
        }
    ]
}
