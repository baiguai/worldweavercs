{
    "topics" : [
        {
            "pattern" : "\\b(?:conf.repeat.types)\\b",
            "title" : "Repeat Types",
            "string" : "
File:
~/Config/ReapeatTypes.json

This configuration file defines the types of repeat actions that can
be utilized by WorldWeaver (WW). Since the logic for these is coded within
WW it is a good idea to no modify this file.
"
        }
    ]
}
